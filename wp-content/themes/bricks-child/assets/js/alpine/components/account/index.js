/**
 * Account Components Registry
 * Centralized registration of all account-related Alpine components
 * 
 * Note: Most account features use stores instead of components
 * - userAddress: Alpine.store('userAddress')
 * Additional components can be added here as needed
 */

export function registerAccountComponents() {
    // Account components will be registered here
    // Currently most account features use stores (userAddress, etc.)
    
    // Future components can be added like:
    // Alpine.data('accountComponent', componentFunction);
}
